# ComfyNexus Backend Dependencies
# Python 3.12.x

# GUI Framework
# 6.2 起 winforms 后端的 create_window/resize/move 统一接收逻辑像素（DPI 缩放修复），
# 窗口尺寸相关代码依赖该语义，不可降级到 6.0/6.1
pywebview>=6.2,<7.0

# HTTP Server
flask>=3.0.0
flask-cors>=4.0.0

# HTTP Client
requests>=2.31.0

# Async HTTP Client (用于 AI Provider)
aiohttp>=3.8.0

# WebSocket
websockets>=12.0

# System Monitoring
psutil>=5.9.0
GPUtil>=1.4.0

# GPU Monitoring (NVIDIA)
pynvml>=11.5.0

# Windows Management Instrumentation (CPU Temperature on Windows)
wmi>=1.5.1; sys_platform == 'win32'

# .NET Runtime Bridge (for LibreHardwareMonitor)
pythonnet>=3.0.0; sys_platform == 'win32'

# AMD GPU Monitoring (Optional)
# Note: AMD GPU monitoring requires additional setup
# pyamdgpuinfo>=2.1.0; sys_platform == 'win32'

# Encryption (用于 API Key 加密存储)
cryptography>=41.0.0

# Search Engines
ddgs>=9.0.0
google-api-python-client>=2.0.0

# Document Processing
pdfplumber>=0.10.0
python-docx>=1.0.0
openpyxl>=3.1.0
Pillow>=10.0.0

# NSFW Image Classification
onnxruntime>=1.16.0

# Video Processing
opencv-python>=4.8.0

# Translation
deep-translator>=1.11.0

# System Tray
pystray>=0.19.0; sys_platform == 'win32'

# YAML Processing
pyyaml>=6.0

# Version Parsing
packaging>=21.0